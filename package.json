{
  "name": "auth-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"json-server --watch db.json --port 3001\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2",
    "json-server": "^0.17.4",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}